services:
  - type: web
    name: vector-web
    runtime: static
    branch: main
    autoDeploy: true
    buildCommand: cd vector && npm ci && npm run build
    staticPublishPath: vector/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html